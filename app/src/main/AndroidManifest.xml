<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.synappze.stoptimer"
    android:sharedUserId="com.synappze.apps"
    android:sharedUserLabel="@string/shared_user_label"
    android:versionCode="4"
    android:versionName="@string/app_version_name">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.synappze.stoptimer.MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.synappze.stoptimer.StopwatchActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.synappze.stoptimer.TimerActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.synappze.stoptimer.PreferencesActivity"
            android:launchMode="singleTop" />

        <provider
            android:name="com.synappze.stoptimer.providers.StopwatchContentProvider"
            android:authorities="com.synappze.stoptimer.providers.StopwatchContentProvider" />
        <provider
            android:name="com.synappze.stoptimer.providers.TimerContentProvider"
            android:authorities="com.synappze.stoptimer.providers.TimerContentProvider" />

        <receiver android:name="com.synappze.stoptimer.receivers.TimerExpiryBroadcastReceiver" />
        <receiver android:name="com.synappze.stoptimer.MainActivity$MainActivityBroadcastReceiver" />
        <receiver android:name="com.synappze.stoptimer.TimerActivity$TimerActivityBroadcastReceiver" />
        <receiver android:name="com.synappze.stoptimer.receivers.BootCompletedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-sdk android:minSdkVersion="15" />
</manifest>